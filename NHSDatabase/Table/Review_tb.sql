CREATE TABLE Reviews_tb (
  REVIEW_ID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY constraint PK_REVIEWID primary key,
  RANKING VARCHAR2(10) constraint R_RANKING CHECK (RANKING in ('VERY GOOD', 'GOOD', 'OK', 'POOR', 'VERY POOR')),
  REASON VARCHAR2(255) NOT NULL
);